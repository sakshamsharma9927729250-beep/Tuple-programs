# Tuple Programs

This repository contains simple Python examples demonstrating how to use tuples.

- `main.py`: shows tuple creation, indexing, slicing, nested tuples, tuple unpacking, and tuple methods like `count()` and `index()`.

Run `main.py` to see the tuple examples printed to the console.
